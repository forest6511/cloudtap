# test/e2e

End-to-end fixtures that spin up sample targets (via Docker Compose or local
binaries) will live here. Keep each scenario in its own directory, document the
expected tunnels, and prefer hermetic setups that can run inside CI.
